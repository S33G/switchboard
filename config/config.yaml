# Example Switchboard config for central nginx routing.
#
# - hosts[].endpoint is how Switchboard talks to each Docker daemon.
# - host_addresses maps Docker host name -> network address reachable from nginx.
#
# With nginx enabled, running containers get routed at:
#   <container-name>.<host-name>.<defaults.base_domain>

hosts:
  - name: c3po
    endpoint: unix:///var/run/docker.sock
    hostname: c3po.home
  - name: deathstar
    endpoint: context://deathstar
    hostname: deathstar.home

host_addresses:
  c3po: c3po.home
  deathstar: deathstar.home

proxy_mappings:
  netboot.containers.home: c3po/netboot:3000
  pinchflat.containers.home: c3po/pinchflat:8945
  plex.containers.home: c3po/plex:6900
  torrents.containers.home: c3po/qbittorrent:8050
  netbootxyz.containers.home: c3po/netbootxyz:3000
  prometheus.containers.home: c3po/prometheus:9090
  grafana.containers.home: c3po/grafana:3060


defaults:
  base_domain: containers.home
  scheme: http
