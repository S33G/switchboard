# Map directive for WebSocket support in generated vhosts
# Per-container vhosts are generated into:
#   /etc/nginx/conf.d/switchboard.d/*.conf

map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

# Include all switchboard-generated per-container configs
include /etc/nginx/conf.d/switchboard.d/*.conf;
